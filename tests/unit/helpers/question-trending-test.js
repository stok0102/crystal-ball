import { questionTrending } from 'crystal-ball/helpers/question-trending';
import { module, test } from 'qunit';

module('Unit | Helper | question trending');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionTrending([42]);
  assert.ok(result);
});
